# Path to the cloned plane-alert-db-normalized repository on the host.
# This directory is bind-mounted into the container so pipeline output
# (updated taxonomy files, normalised CSVs, and logs) is persisted.
REPO_DIR=/opt/plane-alert-db-normalized
